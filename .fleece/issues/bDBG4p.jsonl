{"kind":"create","at":"2026-04-21T09:34:12.384421+00:00","by":"nboey@northrop.com.au","issueId":"bDBG4p","data":{"id":"bDBG4p","title":"Use proper event sourcing pattern with changes, Git hook and CI action","description":"I would like to migrate the Fleece application away from the current system of having multiple hashed \u0060.fleece/issues_{hash}.jsonl\u0060 files to represent issues that are merged together to having a single \u0060.fleece/issues.jsonl\u0060, with changes kept in \u0060.fleece/changes/change_{hash}.jsonl\u0060 files - an event-sourcing system. I am doing this as the \u0060issues_{hash}.jsonl\u0060 don\u0027t merge nicely, and result in very large PR changes as entire files are being added and deleted - the changes would show exactly which changes are occurring per commit.\n\nThis is essentially an event-sourcing pattern, with changes that are projected on the main branch. It should work as follows:\n\n- The issues.jsonl file should just contain the full set of current issues up to the last projection. The per-property modification properties (timestamp and user) should be removed, with only the main issue data held in this file.\n- The change_{hash}.jsonl file should contain two types of data: a property change and an issue change. The property change consists of an issue ID, property name and value that has been changed. This needs to account for properties that are based on arrays (like the parent issue IDs or tags), so should also include metadata that indicates whether a particular array property value is inserted or removed in the change as well as just editing the value. The issue change would consist of a new issue ID and the properties that accompany the IDs.\n- When a change is made (either by the Fleece CLI or by the Core library directly), this should append the change into the relevant change_{hash}.jsonl file. There may be multiple changes at any one time in this folder as they are only merged periodically, so I would like to consider how the exact change_{hash}.jsonl can be found that reduces the likelihood of merge conflicts. We could use the previous commit ID, but this would cause conflicts for two branches from the same commit. We could use a hash of the branch name and the previous commit ID, but this would cause conflicts if the same branch name is used on two different machines (this may be OK, as this would naturally cause issues in Git anyway with divergent branches). Think about this. Perhaps a random GUID and an additional .gitignored file that tracks the current GUID that is \u0022active\u0022 makes sense, but I am not sure what the risks are for the GUID being lost. This may be mitigated by the next point.\n- When a commit is made, the change_{hash}.jsonl file should be locked to that commit, and the change sin the following commit will open a new change_{hash}.jsonl. I think that this could be done with a pre-commit Git hook that is installed with the \u0060fleece install\u0060 command, but one concern is that the commit ID isn\u0027t known until after the commit takes place, so we would again be relying perhaps on a hash of the previous commit and branch name.\n- Periodically, we should project the changes into the issues.jsonl file and delete all changes up to that point. To do this, the \u0060fleece install\u0060 action should also create a GitHub action that runs a \u0060fleece merge\u0060 once per day that performs this action and creates a new commit. This means that any incoming PR or merge would have to rebase onto this commit and therefore pull the merge, although perhaps this would result in issues if pushing directly to main or merging without having to be at the tip. I am OK if this enforces a particular GitHub usage style.\n- The projection should just run through the change files in the order that they were committed. Note that this system should allow for squash-merges, so the exact commit history may not be clear from the main branches point of view. Consider how one might compensate for this.\n- This update should also include a migration command to migrate from the old system to the new system. This may require doubling up on DTOs and classes for a while, but I would like it to be clear in the codebase that the new DTOs are the primary ones - I am OK with renaming the existing ones or putting them in another namespace.\n\nI want to implement this whole feature, but first I would like to see the format of both .jsonl files and discuss the open questions above to agree on before continuing.","status":"Progress","type":"Task","linkedIssues":[],"parentIssues":[],"assignedTo":"nboey@northrop.com.au","tags":["openspec=event-sourced-issues"],"workingBranchId":"event-sourced-issues","executionMode":"Series","createdBy":"testuser","createdAt":"2026-04-21T09:34:12.384421\u002B00:00","lastUpdate":"2026-05-01T02:37:10.8844177\u002B00:00"}}
